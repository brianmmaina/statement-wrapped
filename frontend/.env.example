# Optional: Call backend directly instead of using the dev proxy.
# Use when you get 404 errors (e.g. running preview or a different dev setup).
# VITE_API_URL=http://localhost:8000

# Optional: Required when backend has API_KEY set.
# VITE_API_KEY=your-secret-key
